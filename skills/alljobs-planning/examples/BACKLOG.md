# Backlog Example

## AJ-B-001: Pure Markdown Parsers

```yaml alljobs
id: AJ-B-001
work_mode: implementation
phase: phase-1
status: ready
priority: P0
rank: 100
dependencies: []
done_when: Parsers return typed objects and isolate section errors.
```

Section splitting and YAML parsing.

> New or substantive Backlog items are proposed to a repository agent for review, stable-ID selection, validation, and the repository's normal Git workflow. AllJobs only changes the `priority` and `rank` fields of existing items after an explicit Human Gate.
