# Tasks Example (Native Format)

> Note: Code repositories are not required to keep Tasks files in their repo.
> In V1, the `git-markdown` provider only reads `docs/ROADMAP.md` and `docs/BACKLOG.md`.
> Native tasks are maintained directly in AllJobs under `data/tasks/<slug>.md`.

## AJ-T-001: Implement Parser Tests

```yaml alljobs
id: AJ-T-001
project: alljobs
status: doing
backlog: AJ-B-001
source:
  provider: native
```
