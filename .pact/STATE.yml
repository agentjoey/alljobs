project: alljobs
working_tree_holder: null
agents:
  - { id: claude, roles: [orchestrator, reviewer] }
  - { id: claude, roles: [worker] }
  - { id: codex, roles: [worker] }
  - { id: gemini, roles: [worker] }
  - { id: kimi, roles: [worker] }
  - { id: opencode, roles: [worker] }
  - { id: ${PACT_AGENT_ID:-}, roles: [worker] }
features:
  - id: workbench-v1
    branch: 
    status: in_progress
    tasks:
      - id: data-layer
        owner: kimi
        status: accepted
        reviewer: claude
        spec: .agent/frontend-design/alljobs-workbench-v1/tasks/T1-data-layer.md
        evidence: 返工两处 + 小项均已解决，验收命令全绿：  【1】日志 frontmatter 剥离（read.ts:85-115）：readLogs 先 matter(raw) 取 .content 再逐行解析；行号按原文件真实行号计（lineOffset = 原文件行数 - content 行数）。frontmatter 损坏时 matter 抛错则回退按原文解析（坏行自然进 ProofIssue）。新增测试「日志文件可带 frontmatter：剥离后解析，行号按原文件计」——注入 ---/mood: tired/--- 后零误报，仅第 6 行坏行进 ProofIssue 且行号正确（修复前会产生 3 条假 issue）。data/README.md 日志一节已补「frontmatter 可选（存在则被忽略）」。  【2】dueSoon 定案为方案 (a)（derive.ts:56-59）：去掉 daysToDue >= 0 下界，逾期（daysToDue 为负）也算 dueSoon，只卡上界 5 天。代码注释与 data/README.md 派生规则均写明「含已过期，无下界：逾期比临近到期更该被看见」。新增逾期单测：active 项目 due=2026-08-08、今天有日志（不 stale 不 blocked）→ dueSoon=true 且注意清单返回 [overdue, dueSoon]（修复前返回 []）；既有边界测试更新（逾期 1 天=true）。种子 e2e 不受影响（种子 due 均在未来）：tradelinks→petcare-app→eastern-astrology-mvp 断言仍通过。  【小项】read.ts 顶部加 import "server-only"（已装依赖）。该包在纯 node 下 import 即抛错，故新增 vitest.config.mts 用 resolve.alias 将 server-only 映射到 lib/data/empty-stub.ts（空模块），Next 构建期语义不受影响（build 已验证通过）。  验收输出： - npm run lint：0 errors，1 warning（scripts/shot.mjs:49 既有，非本任务文件） - npm run build：Next.js Turbopack ✓ Compiled / ✓ TypeScript / ✓ 静态页生成 - npm test：Test Files 4 passed (4)，Tests 35 passed (35)（较上轮 +2：frontmatter 用例 + 逾期用例）  变更文件：lib/data/read.ts、lib/data/derive.ts、lib/data/read.test.ts、lib/data/derive.test.ts、data/README.md、vitest.config.mts（新增）、lib/data/empty-stub.ts（新增）、package.json / package-lock.json（server-only 依赖）。未碰 UI 与 app/ 页面，未碰其他 task 文件。
      - id: pages
        owner: kimi
        status: accepted
        reviewer: claude
        spec: .agent/frontend-design/alljobs-workbench-v1/tasks/T2-pages.md
        evidence: 打回项【日分组间距丢失】已修复，按建议最小改动：app/log/log-view.tsx 与 app/projects/[slug]/detail-view.tsx 的日组包裹 <div key> 换成 <Fragment key>（各加 import { Fragment } from "react"），dayhead/sheet 回到 main.ledger 平铺兄弟层，未改任何 CSS。  CDP 实测（修复后最终 build + next start，自建测量脚本读 computed margin-top 与 prevSibling 底边→dayhead 顶边 gap）： - /log @1440：4 个日组 gap 30/30/30/30（首个 prev=.filters，与 mockup 同形） - /log @390：30/30/30/30 - /projects/tradelinks @1440：2 个日组 gap 0/30（首个 prev=h2.sr-only，首个 first-of-type 归零） - /projects/tradelinks @390：0/30 - mockup 对照（file:// 同脚本同口径）：mockup/log.html 30/30/30；mockup/project.html 首个 0（prevClass=null，first-of-type 命中）、其后 30/30 —— 实现与 mockup 逐值一致。  顺带确认：.empty-note--flush（空日不占页。）gap=0、marginTop=0px、内 padding-top 12px，与 mockup .empty-note 规则一致（mockup 用 inline padding-left:0，等价 --flush）；临时注入 data/log/2026-06-20.md 使月份折叠 sheet 出现后实测：.sheet--months 紧贴上一张 sheet（gap=0，无 margin 规则），flush 注记跟在其后 gap=0，与修复前结构位置不变（测完已删临时文件；该次注入同时旁证 next start 下改 data 即时反映）。390 两端 scrollWidth==clientWidth==390，无横向滚动。  TDD：先在 app/views.test.tsx 加 2 条结构断言（日组不被包裹：html 不含 <div><div class="section-head dayhead"，且相邻日组出现 </div><div class="section-head dayhead" 平铺形态），确认红（2 failed），再改实现转绿。  验收命令（全部跑在最终代码之后）： - npm run lint：0 errors，1 warning（scripts/shot.mjs:49 既有，非本任务文件） - npm run build：Turbopack ✓ Compiled / ✓ TypeScript / ✓ 静态生成，四路由全 ƒ（/、/log、/projects、/projects/[slug]） - npm test：Test Files 6 passed (6)，Tests 83 passed (83)（较上轮 +2 结构断言） - grep -r cda17d0d .next：命中 13 个文件（含 _not-found.html/.rsc 中的契约注释全文）  截图（修复后最终 build，next start -p 3457，shot.mjs scale=2 与上轮一致；3456 被上轮遗留 next-server 占用故换端口，未动该进程）：重拍 log-1440 / log-390 / detail-1440 / detail-390 四张存 impl-screens/，日组 30px 呼吸肉眼可见；overview/projects 四张沿用。  非阻塞说明（有意省略，勿再查）：(a) 校对横幅无「打开文件」链接——mockup 中即 href="#" 占位，浏览器本就打不开本地文件，保持现状是有意省略；(b) 详情头 [repo] 为纯文本——本地路径不可点，[vault]/[url] 仍是真链接，保持现状。
        deps: [data-layer]
      - id: quickadd
        owner: kimi
        status: assigned
        reviewer: claude
        spec: .agent/frontend-design/alljobs-workbench-v1/tasks/T3-quickadd.md
        evidence: null
        deps: [data-layer, pages]
      - id: deploy-docs
        owner: kimi
        status: in_progress
        reviewer: claude
        spec: .agent/frontend-design/alljobs-workbench-v1/tasks/T4-deploy-docs.md
        evidence: null
