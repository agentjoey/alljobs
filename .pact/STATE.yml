project: alljobs
working_tree_holder: null
agents:
  - { id: claude, roles: [orchestrator, reviewer] }
  - { id: claude, roles: [worker] }
  - { id: codex, roles: [worker] }
  - { id: gemini, roles: [worker] }
  - { id: kimi, roles: [worker] }
  - { id: opencode, roles: [worker] }
  - { id: ${PACT_AGENT_ID:-}, roles: [worker] }
features:
  - id: workbench-v1
    branch: 
    status: in_progress
    tasks:
      - id: data-layer
        owner: kimi
        status: accepted
        reviewer: claude
        spec: .agent/frontend-design/alljobs-workbench-v1/tasks/T1-data-layer.md
        evidence: 返工两处 + 小项均已解决，验收命令全绿：  【1】日志 frontmatter 剥离（read.ts:85-115）：readLogs 先 matter(raw) 取 .content 再逐行解析；行号按原文件真实行号计（lineOffset = 原文件行数 - content 行数）。frontmatter 损坏时 matter 抛错则回退按原文解析（坏行自然进 ProofIssue）。新增测试「日志文件可带 frontmatter：剥离后解析，行号按原文件计」——注入 ---/mood: tired/--- 后零误报，仅第 6 行坏行进 ProofIssue 且行号正确（修复前会产生 3 条假 issue）。data/README.md 日志一节已补「frontmatter 可选（存在则被忽略）」。  【2】dueSoon 定案为方案 (a)（derive.ts:56-59）：去掉 daysToDue >= 0 下界，逾期（daysToDue 为负）也算 dueSoon，只卡上界 5 天。代码注释与 data/README.md 派生规则均写明「含已过期，无下界：逾期比临近到期更该被看见」。新增逾期单测：active 项目 due=2026-08-08、今天有日志（不 stale 不 blocked）→ dueSoon=true 且注意清单返回 [overdue, dueSoon]（修复前返回 []）；既有边界测试更新（逾期 1 天=true）。种子 e2e 不受影响（种子 due 均在未来）：tradelinks→petcare-app→eastern-astrology-mvp 断言仍通过。  【小项】read.ts 顶部加 import "server-only"（已装依赖）。该包在纯 node 下 import 即抛错，故新增 vitest.config.mts 用 resolve.alias 将 server-only 映射到 lib/data/empty-stub.ts（空模块），Next 构建期语义不受影响（build 已验证通过）。  验收输出： - npm run lint：0 errors，1 warning（scripts/shot.mjs:49 既有，非本任务文件） - npm run build：Next.js Turbopack ✓ Compiled / ✓ TypeScript / ✓ 静态页生成 - npm test：Test Files 4 passed (4)，Tests 35 passed (35)（较上轮 +2：frontmatter 用例 + 逾期用例）  变更文件：lib/data/read.ts、lib/data/derive.ts、lib/data/read.test.ts、lib/data/derive.test.ts、data/README.md、vitest.config.mts（新增）、lib/data/empty-stub.ts（新增）、package.json / package-lock.json（server-only 依赖）。未碰 UI 与 app/ 页面，未碰其他 task 文件。
      - id: pages
        owner: kimi
        status: changes_requested
        reviewer: claude
        spec: .agent/frontend-design/alljobs-workbench-v1/tasks/T2-pages.md
        evidence: 评审 4 条必修 + 2 条应修全部解决，复验全部来自修复后的最终 build（lint/build/test 重跑于最终代码之后）。  【必须修复 1 · axe aria-allowed-attr critical】chips 保持链接语义，aria-pressed 全部改 aria-current（未选中不渲染属性）：app/projects/projects-view.tsx、app/log/log-view.tsx；globals.css 选中态选择器改 .chip[aria-current="true"]。「更多›」toggle chip 不再带任何状态属性。新增测试断言 html 不含 aria-pressed（views.test.tsx ×2）。 axe 复验（axe-core 4.x 经 CDP 注入生产页，最终 build + next start）：   / @1440 v=0 (passes 39)  /projects @1440 v=0 (34)  /log @1440 v=0 (32)  /projects/tradelinks @1440 v=0 (35)   / @390 v=0 (39)  /projects @390 v=0 (34)  /log @390 v=0 (32)  /projects/tradelinks @390 v=0 (35)   八组 violations 全 0（incomplete 仅 color-contrast 需人工项 0-1，无 serious+）。  【必须修复 2 · link-in-text-block serious】detail-view.tsx 「更早记录见日志 · 按月归档 →」整句做成独立链接（.empty-note-link，颜色继承、hover 下划线），不再嵌在文本块里。axe /projects/tradelinks 两端 v=0。新增测试断言整句为 <a> 且无「更早记录见 <a」嵌套形态。  【必须修复 3 · 404 返回 200】[slug]/page.tsx 改调 notFound()；内容（查无此页 + 最近 slug 索引）搬到同级 not-found.tsx（force-dynamic，内部 readLedger()，索引保留：实测 404 页含 /projects/tradelinks、/projects/pactify-apps、/projects/alljobs 链接）。not-found 拿不到 params，故文案不再指名 slug（not-found-view.tsx slug 改可选）。关键坑：app/loading.tsx 与 app/projects/loading.tsx 这类祖先 loading 会让 Suspense 抢先发 200 头（soft 404）——骨架已收进路由组：(overview)/loading.tsx、(list)/loading.tsx、log/loading.tsx，[slug] 祖先链无任何 loading。   复验：curl /projects/no-such-slug → 404；页面含「查无此页」与最近 slug 索引。  【必须修复 4 · /log 过滤为空说假话】log-view.tsx 区分 data.entries.length===0（真空账→保留教 schema 引导）与过滤后为空（「没有同时满足 slug + @agent 的日志。清除过滤」，chip--inline 链接 /log）。新增测试：alljobs+joey 组合断言新文案且不含「还没有日志」；空账带过滤参数仍出引导。   复验：curl "/log?slug=alljobs&agent=joey" 命中「没有同时满足」「清除过滤」。  【应修 5 · 快速添加行折行】globals.css 两处：select flex:none→flex:0 1 auto + min-width:0 + max-width:8rem；input basis 140→96（Chrome 对原生 select 换行判定有余量，basis 140 时 425.3px 行仍折）。CDP 实测修复后一行：input 130.7 + select 128 + select 64 + 落账 51.3，四个 top 同行。overview-1440.png 重拍确认落账同行；390 下仍按 mockup wrap，无横向滚动。  【不阻塞 (a)】attentionWhy（components/ledger/lib.ts）blocked 的 why 只取 now 首句（firstSentence：到首个句读/换行），TradeLinks 注意清单回到单行；新增单测。(b) [slug]/page.tsx 改 generateMetadata，标题=项目名（查无此页时回退）。  【lint/build/test（最终代码）】lint 0 errors（1 既有 warning scripts/shot.mjs）；build Next 16.3 Turbopack ✓，四路由全 ƒ；vitest 6 files / 81 tests 全过（较上轮 +3：chip aria-current×2、整句链接、log 过滤为空、why 首句等）。截图 8 张全部重拍自最终 build（log 两张与上次字节相同，因该页无 quickadd）。 变更文件：app/globals.css、app/log/log-view.tsx、app/projects/projects-view.tsx、app/projects/[slug]/{page,detail-view,not-found-view,not-found.tsx(新)}、app/(overview)/{page,loading}.tsx、app/projects/(list)/{page,loading}.tsx、app/log/loading.tsx(新)、components/ledger/lib.ts、app/views.test.tsx、components/ledger/ledger.test.tsx、impl-screens 8 张。data/ 无改动。
        deps: [data-layer]
      - id: quickadd
        owner: kimi
        status: assigned
        reviewer: claude
        spec: .agent/frontend-design/alljobs-workbench-v1/tasks/T3-quickadd.md
        evidence: null
        deps: [data-layer, pages]
      - id: deploy-docs
        owner: kimi
        status: in_progress
        reviewer: claude
        spec: .agent/frontend-design/alljobs-workbench-v1/tasks/T4-deploy-docs.md
        evidence: null
