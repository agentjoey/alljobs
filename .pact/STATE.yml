project: alljobs
working_tree_holder: null
agents:
  - { id: claude, roles: [orchestrator, reviewer] }
  - { id: claude, roles: [worker] }
  - { id: codex, roles: [worker] }
  - { id: gemini, roles: [worker] }
  - { id: kimi, roles: [worker] }
  - { id: opencode, roles: [worker] }
  - { id: ${PACT_AGENT_ID:-}, roles: [worker] }
features:
  - id: workbench-v1
    branch: 
    status: in_progress
    tasks:
      - id: data-layer
        owner: kimi
        status: accepted
        reviewer: claude
        spec: .agent/frontend-design/alljobs-workbench-v1/tasks/T1-data-layer.md
        evidence: 返工两处 + 小项均已解决，验收命令全绿：  【1】日志 frontmatter 剥离（read.ts:85-115）：readLogs 先 matter(raw) 取 .content 再逐行解析；行号按原文件真实行号计（lineOffset = 原文件行数 - content 行数）。frontmatter 损坏时 matter 抛错则回退按原文解析（坏行自然进 ProofIssue）。新增测试「日志文件可带 frontmatter：剥离后解析，行号按原文件计」——注入 ---/mood: tired/--- 后零误报，仅第 6 行坏行进 ProofIssue 且行号正确（修复前会产生 3 条假 issue）。data/README.md 日志一节已补「frontmatter 可选（存在则被忽略）」。  【2】dueSoon 定案为方案 (a)（derive.ts:56-59）：去掉 daysToDue >= 0 下界，逾期（daysToDue 为负）也算 dueSoon，只卡上界 5 天。代码注释与 data/README.md 派生规则均写明「含已过期，无下界：逾期比临近到期更该被看见」。新增逾期单测：active 项目 due=2026-08-08、今天有日志（不 stale 不 blocked）→ dueSoon=true 且注意清单返回 [overdue, dueSoon]（修复前返回 []）；既有边界测试更新（逾期 1 天=true）。种子 e2e 不受影响（种子 due 均在未来）：tradelinks→petcare-app→eastern-astrology-mvp 断言仍通过。  【小项】read.ts 顶部加 import "server-only"（已装依赖）。该包在纯 node 下 import 即抛错，故新增 vitest.config.mts 用 resolve.alias 将 server-only 映射到 lib/data/empty-stub.ts（空模块），Next 构建期语义不受影响（build 已验证通过）。  验收输出： - npm run lint：0 errors，1 warning（scripts/shot.mjs:49 既有，非本任务文件） - npm run build：Next.js Turbopack ✓ Compiled / ✓ TypeScript / ✓ 静态页生成 - npm test：Test Files 4 passed (4)，Tests 35 passed (35)（较上轮 +2：frontmatter 用例 + 逾期用例）  变更文件：lib/data/read.ts、lib/data/derive.ts、lib/data/read.test.ts、lib/data/derive.test.ts、data/README.md、vitest.config.mts（新增）、lib/data/empty-stub.ts（新增）、package.json / package-lock.json（server-only 依赖）。未碰 UI 与 app/ 页面，未碰其他 task 文件。
      - id: pages
        owner: kimi
        status: changes_requested
        reviewer: claude
        spec: .agent/frontend-design/alljobs-workbench-v1/tasks/T2-pages.md
        evidence: 续上轮半成品补齐后全部验收通过。  【修复/补齐（本轮）】globals.css 头部注释内 `--st-*/--ag-*` 导致 CSS 注释提前闭合（Turbopack 报 Unexpected token）→ 改写为「--st-* 系列」；app/projects/loading.tsx 导入路径 ../../../→../../；补 app/log/page.tsx 与 app/projects/[slug]/page.tsx（searchParams/params 驱动，force-dynamic，slug 不存在渲染含最近索引的 ProjectNotFound）；log-view 去掉未用 LogEntry 导入。  【验收命令输出】 - npm run lint：0 errors，1 warning（scripts/shot.mjs:49 既有，非本任务文件） - npm run build：Next 16.3 Turbopack ✓ Compiled / ✓ TypeScript；四路由全部 ƒ（Dynamic server-rendered）：/、/log、/projects、/projects/[slug] - npm test：Test Files 6 passed (6)，Tests 78 passed (78)（app/views.test.tsx 覆盖四视图状态矩阵 + components/ledger/ledger.test.tsx 组件测试） - 契约注释存活：grep -r cda17d0d .next 命中 .next/server/app/_not-found.html、.next/server/app/_not-found.rsc 等（ContractComment 以 hidden div 承 HTML 注释置于 body 首子节点）  【动态渲染验证（改 data 刷新即变）】 - next start -p 3456：向 data/log/2026-08-11.md 追加一行 → curl /log 命中新条目；还原后命中 0 - next dev -p 3457：同样追加 → 命中；还原后 0（数据文件均已还原，git diff data/ 为空）  【截图】npm run build && npm run start -- -p 3456 后 scripts/shot.mjs 拍 8 张存 .agent/frontend-design/alljobs-workbench-v1/impl-screens/：overview/projects/detail/log 各 1440(scale2,desktop)+390(scale2,mobile)。目检：对开双页/红栏线/戳形/索引标签与 mockup 一致；390px 内容满宽无横向滚动。  【交付物对照】token 移植（@theme 承接 ledger.css：shadcn 语义名+--rule-red/--feint/--st-*/--ag-*/--t0..t4，Geist Mono 走 next/font，无 Google CDN）；layout 方向契约注释；components/ledger/ 共享组件（Masthead/Sheet/Row/Stamp/AgentMark/Tally/DateStamp/ProofBanner/SectionHead）；四路由 server components + force-dynamic；状态矩阵（空账引导/无事行/今日无日志/过滤无结果+清除/无活动/缺 Now 占位/done 减淡/校对横幅/loading.tsx 骨架）均有测试覆盖；a11y（每页 h1、dayhead 标题元素、aria-current/aria-pressed、focus-visible、reduced-motion）见 globals.css 与组件。  变更文件：app/globals.css、app/layout.tsx、app/page.tsx、app/loading.tsx、app/overview-view.tsx、app/views.test.tsx、app/log/{page,log-view}.tsx、app/projects/{page,loading,projects-view}.tsx、app/projects/[slug]/{page,detail-view,not-found-view}.tsx、components/ledger/**（本轮新增）、.agent/.../impl-screens/（8 张截图证据）。未碰 lib/data（T1）与其他 task 文件。
        deps: [data-layer]
      - id: quickadd
        owner: kimi
        status: assigned
        reviewer: claude
        spec: .agent/frontend-design/alljobs-workbench-v1/tasks/T3-quickadd.md
        evidence: null
        deps: [data-layer, pages]
      - id: deploy-docs
        owner: kimi
        status: in_progress
        reviewer: claude
        spec: .agent/frontend-design/alljobs-workbench-v1/tasks/T4-deploy-docs.md
        evidence: null
